/**
 * Hero background photo. Referenced both by `landing.css` (as the painted
 * background layer) and by the preload hint in the page — keep the two in
 * sync when changing the asset.
 */
export const HERO_IMAGE_SRC = '/images/landing/home-hero-skincare-pile.webp'
