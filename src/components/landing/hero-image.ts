/**
 * Hero background photo — the Ken Burns cinematic zoom layer behind the
 * headline. Referenced both by `landing.css` (as the painted background
 * layer on `.hero::before`) and by the preload hint in the page — keep the
 * two in sync when changing the asset.
 */
export const HERO_IMAGE_SRC = '/images/landing/skinaudit-hero.jpg'
